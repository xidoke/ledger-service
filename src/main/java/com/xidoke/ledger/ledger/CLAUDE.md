# ledger package — CLAUDE.md

Placeholder for Phase 1. Will document the core ledger primitives — `LedgerEntry` immutable append-only, posting service, and the reconciliation job (Σ entries == balance). ADR-005, ADR-006, ADR-016.

See repository root `CLAUDE.md` for project-wide conventions.
