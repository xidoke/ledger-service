# outbox package — CLAUDE.md

Placeholder for Phase 1. Will document the transactional outbox pattern — INSERT outbox row in the same DB transaction as ledger entries, then a poller publishes (log-only at Nấc 0, broker at Nấc 2+). ADR-013, ADR-015.

See repository root `CLAUDE.md` for project-wide conventions.
